# Strava history IS committed (public repo, so the deployed site has data).
# The slimmed activity data has no GPS/route coordinates.
# But NEVER commit OAuth tokens or API secrets (both live in data/private/):
data/private/
*.env

# Python
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store
*.swp
